geniki:
  enabled: false
  use_proxy: false
  image:
    repository: dpikilidis/geniki-tracker
    tag: latest
  loglevel: INFO

autoscaling:
  horizontal:
    min_replicas: 1
    max_replicas: 10
  vertical:
    enabled: false
    mode: "Off"  # Possible values: "Auto", "Off", "Initial", "Recreate""  