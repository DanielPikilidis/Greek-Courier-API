elta:
  enabled: true
  use_proxy: false
  name: elta-tracker
  image:
    repository: dpikilidis/elta-tracker
    tag: latest
  logpath: /tmp
  logname: elta-tracker.log
  loglevel: INFO

autoscaling:
  horizontal:
    min_replicas: 2
    max_replicas: 20
  vertical:
    enabled: false
    mode: "Off"  # Possible values: "Auto", "Off", "Initial", "Recreate""  